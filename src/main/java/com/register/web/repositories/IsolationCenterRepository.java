package com.register.web.repositories;

import com.register.web.models.IsolationCenter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IsolationCenterRepository extends JpaRepository<IsolationCenter, Long> {
}
